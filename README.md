# Cours

Voici une liste d'exercices histoire que tu progresse en algo

## Commandes git usuelles

### Créer une branche

```bash
git checkout -b {{ta branche}} origin/{{ta branche originelle}}
```

### Workflow

```bash
git add --all

git commit -m "{{ton message}}"

git push origin {{ta branche}}
```
